# Tasks

## 1. 片段模型与可定位来源

- [ ] 1.1 扩展数据库初始化，增加片段元数据、索引状态和 FTS5 结构；在已有数据库上连续运行初始化两次，并验证种子材料/现有知识条目保留且新结构无重复。
- [ ] 1.2 实现段落优先、带重叠的确定性分块，并记录稳定 chunk ID、内容哈希及原文 Unicode 字符偏移；单元测试覆盖中文、Markdown、跨段边界和偏移切片能还原命中原文。
- [ ] 1.3 增加配置化的中文分词、Chroma 持久路径和本地 embedding 模型依赖/加载适配；通过替身 embedding 的单元测试验证无网络依赖时可初始化，并验证 Compose 配置将索引与模型缓存放在持久化数据卷。

## 2. 混合检索与索引生命周期

- [ ] 2.1 实现幂等索引构建与重建流程，为已有 `knowledge_entries` 补齐关键词和向量索引；对同一数据库重复运行重建并验证结果数、chunk ID 和来源记录保持稳定。
- [ ] 2.2 实现关键词与向量候选召回，两路均按会话班级过滤，并在返回前用 SQLite 校验材料班级和存在性；用 A/B 班区分材料验证关键词、向量及伪造班级参数均无跨班结果。
- [ ] 2.3 实现两路候选的 RRF 融合、重叠片段去重和有界 top-k；使用固定检索/embedding fixture 验证精确词命中与无共享主要关键词的语义近义查询都可返回相关片段，且结果不超过上限。
- [ ] 2.4 将上传、材料改名、删除与索引状态协调起来，并提供失败项重试/全量重建；测试新上传可检索、改名显示新标题、删除后不再命中，以及模拟索引故障后可恢复且无孤儿来源。

## 3. API、安全与工作区体验

- [ ] 3.1 新增登录保护的 `GET /api/knowledge/search`，实现空查询、limit 边界、无结果和稳定来源响应；HTTP 测试验证未登录返回 401、非法输入返回 400、结果含正确片段偏移且班级参数不能越权。
- [ ] 3.2 扩展材料工作区内容检索，展示命中片段、来源标题和预览/下载入口，并区分加载、无结果及错误；前端测试验证来源预览使用安全文本且能够定位对应片段。
- [ ] 3.3 保持 `/api/materials?q=` 的标题筛选和原有角色/班级授权兼容；运行既有材料列表、上传、下载、预览和跨班隔离测试并确认通过。

## 4. Compose 验收与文档

- [ ] 4.1 调整容器启动流程，在数据库初始化后仅 reconcile 缺失/变更/失败索引，再启动应用；构建并启动 Compose，验证首次模型加载完成后健康检查成功，且重复重启不无条件重算全部向量。
- [ ] 4.2 扩展 Compose 验收脚本覆盖正文检索、语义检索、A/B 班隔离、来源预览授权和 down/up 后索引持久性；在本地 Docker Compose 环境运行脚本并确认所有检索断言通过。
- [ ] 4.3 更新 README 的本地模型首次加载、持久化目录、索引重建/故障恢复及检索 API 说明；执行完整测试集、Compose 验收和 `openspec validate add-traceable-class-scoped-retrieval --strict` 并记录通过结果。
