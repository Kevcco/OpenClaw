# Tasks

## 1. 仓库与应用骨架（T1）

- [ ] 1.1 核对根目录 `README.md` 的价值、核心场景、非目标三行与 proposal 一致，并通过人工逐项对照验证内容无越界。
- [ ] 1.2 添加 `requirements.txt`、`run.py` 和 `app/` 包骨架，固定 Flask、bcrypt 与 gunicorn 依赖；运行 `pip install -r requirements.txt` 及 `python -c "import app"` 验证安装和导入成功。
- [ ] 1.3 建立自动化测试目录、隔离的测试数据库/上传目录 fixture 和 Flask test client；运行空测试套件并确认测试进程不读写正式 `data/`、`uploads/`。

## 2. 数据模型与种子（T2）

- [ ] 2.1 实现数据库连接与 schema 初始化，创建班级、用户、讲义、作业、助手、技能、材料和知识库条目结构及必要外键/索引；运行 `scripts/init_db.py` 后用 SQLite schema 查询验证所有结构存在。
- [ ] 2.2 实现可重复执行的种子逻辑，写入班级 A/B、教师 A、学生 A1/B1 和两班可区分的材料；连续执行初始化两次并查询计数，验证数据完整且没有重复。
- [ ] 2.3 使用 bcrypt 生成所有种子用户的 `password_hash`，禁止明文密码列；查询数据库并用 bcrypt 分别校验正确与错误密码，验证库中值不是任何预置明文。
- [ ] 2.4 实现参数化数据库 helper，并让材料与知识库查询显式接收可信 `class_id`；以班级 A/B 分别调用 helper，验证结果只包含对应班级数据。

## 3. 登录、会话与认证（T3）

- [ ] 3.1 实现从环境变量读取 `SECRET_KEY` 的应用工厂以及 session Cookie 安全属性；在缺少变量时启动应用并验证进程明确失败，在提供变量时验证应用正常创建。
- [ ] 3.2 实现 `/login` 页面和账号密码登录，session 写入 `user_id`、`role`、`class_id`；用教师 A、学生 A1/B1 登录测试验证三类会话字段正确。
- [ ] 3.3 实现错误密码的统一失败响应与登出清理；测试错误密码不建立 session、不泄露哈希，登出后原 session 不能继续访问受保护资源。
- [ ] 3.4 实现统一认证保护：未登录页面请求 `302` 到 `/login`，未登录 API 请求返回 `401` JSON；测试两类响应均不包含任何材料信息。

## 4. 班级隔离（T4）

- [ ] 4.1 实现材料列表页面/API，仅使用 session 的 `class_id` 查询数据库；学生 A1 与学生 B1 分别请求列表，验证只出现本班可区分标题。
- [ ] 4.2 实现按材料 ID 的读取、修改和删除边界，将资源不存在与跨班访问统一返回 `404`；教师 A 请求 B 班材料 ID，验证响应为 `404` 且不含 B 班标题、正文、路径、存储键或班级信息。
- [ ] 4.3 拒绝客户端班级参数覆盖会话；在 query、path 与 body 中伪造 B 班标识进行列表和写入测试，验证 A 班会话始终只能操作 A 班数据。
- [ ] 4.4 增加班级隔离回归测试，覆盖教师和学生、列表与单条资源、合法分页和搜索参数；运行相关测试并验证无跨班记录泄露。

## 5. 角色权限与上传入库（T5）

- [ ] 5.1 在任何文件或数据库写入前实施教师角色检查；以学生 A1 POST 合法文件，验证返回 `403` 且 `materials`、`knowledge_entries` 与上传目录在请求前后完全不变。
- [ ] 5.2 实现 `.txt`/`.md` 扩展名、空文件、文件名与 UTF-8 解析校验，以及按班级暂存和安全最终路径；针对非法扩展名、空文件和解析失败运行测试，验证返回 `400` 且没有孤立文件。
- [ ] 5.3 实现教师上传事务：材料与知识库条目使用同一 session `class_id` 写入并关联，成功后移动到最终路径并返回 `201` 和 `material_id`；教师 A 上传测试验证两表各新增一条 A 班记录且文件存在。
- [ ] 5.4 实现上传失败补偿，覆盖解析错误、数据库异常与最终文件移动失败；对每个失败点注入异常并验证事务回滚、临时文件清理且无不完整记录。
- [ ] 5.5 让材料列表从数据库读取新记录；教师 A 上传后分别以教师 A、学生 A1 和学生 B1 刷新列表，验证 A 班两者可见、B 班不可见且学生仍不能执行写操作。

## 6. Docker Compose 与健康检查（T6）

- [ ] 6.1 添加 `Dockerfile`、`docker-compose.yml`、`.env.example` 与容器启动入口，映射 `8080:8080` 并挂载 `./data`、`./uploads`；运行 Compose 配置校验，确认真实密钥未进入版本文件。
- [ ] 6.2 实现无需认证的 `GET /health` 并配置 Compose healthcheck；使用无 Cookie 请求验证返回 HTTP 200、JSON `{"status":"ok"}` 且不重定向。
- [ ] 6.3 在容器启动时检测并初始化缺失数据库；从空 `data/` 启动 Compose，等待 healthcheck 通过后验证三名预置用户可登录。
- [ ] 6.4 验证持久化：教师上传一条材料后执行 `docker compose down` 再 `up`（不删除挂载数据），确认用户仍可登录且材料与知识库记录仍存在。
- [ ] 6.5 在 README 补充从 `.env.example` 创建 `.env`、设置 `SECRET_KEY`、Compose 启动、登录 URL、health URL 与跨班统一 `404` 约定；由干净环境按 README 操作并验证登录页和健康检查可访问。

## 7. 规约验证与场景验收（T7）

- [ ] 7.1 运行 `openspec validate add-auth-rbac-class-knowledge --strict`，验证命令退出码为 0 且无 warning/error。
- [ ] 7.2 运行完整自动化测试，验证登录、错误密码、未认证、学生上传 `403`、跨班 `404`、上传成功、失败清理和健康检查场景全部通过。
- [ ] 7.3 手工验收关键用户路径：教师 A 上传材料，学生 A1 可见但只读，学生 B1 不可见；在实验报告或 PR 中逐条记录对应 Scenario 的通过/失败结果与证据。
