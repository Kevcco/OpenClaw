# CampusClaw 项目规则

- 无规约不写代码：任何功能实现前，必须先在 `openspec/changes/` 中形成并评审对应的 proposal、design、spec 与 tasks。
- 规约优先：实现与验收均以 OpenSpec 中可判定的 Requirement、Scenario 和 tasks 为准，口头约定不替代仓库内文件。
- 分阶段交付：第 2 课只允许完善 Markdown 规约，不 Apply、不编写业务页面、不运行真实 Docker Compose；实现与归档留到后续课程。
- 安全边界必须在服务端落实；前端隐藏按钮不能替代角色权限或班级隔离。
- 未经明确要求，不扩大本 change 的范围。
